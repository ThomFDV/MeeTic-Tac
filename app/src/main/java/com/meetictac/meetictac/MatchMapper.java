package com.meetictac.meetictac;

import java.util.ArrayList;
import java.util.List;

public class MatchMapper {
     /*

    public static MatchModel map(MatchDTO weaponDTOList) {
        MatchModel weaponList = new ArrayList<>();
        for (MatchDTO weaponDTO : weaponDTOList) {
            weaponList.add(map(weaponDTO));
        }
        return weaponList;
    }

   /* private static MatchModel map(MatchDTO weaponDTO) {
        MatchModel weapon = new MatchModel();
        weapon.setBraceletUrl(weaponDTO.getBraceletUrl());
        weapon.setDescription(weaponDTO.getDescription());
        weapon.setDialUrl(weaponDTO.getDialUrl());
        weapon.setHousingUrl(weaponDTO.getHousingUrl());
        return weapon;
    }*/
}
